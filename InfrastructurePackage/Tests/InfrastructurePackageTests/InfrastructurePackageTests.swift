import XCTest
@testable import InfrastructurePackage

final class InfrastructurePackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(InfrastructurePackage().text, "Hello, World!")
    }
}
